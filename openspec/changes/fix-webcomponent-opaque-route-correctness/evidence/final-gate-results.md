# Final gate results

## Passed

- HTMX route-policy Node suite: 5 tests passed with zero failures.
- Web-component foundation and HTMX Maven reactor: `BUILD SUCCESS`.
- Java route codec and controller focused tests: `BUILD SUCCESS`.
- Pinned composite-values GraphQL, codec, direct-route, and reconstruction integration: `BUILD SUCCESS`.
- Petclinic integration and Playwright reactor: `BUILD SUCCESS`.
- Petclinic Playwright: 4 tests passed with zero failures, errors, or skips.
- Reference Application ordinary clean package, integration tests, inventory tests, and deterministic fixtures: `BUILD SUCCESS`.
- Clean and incremental capability inventory retained SHA-256 `75ef904a0d4fbc9c915c74866cdbd503743dab589f7525bbab126baf1eaa024a` byte-for-byte.
- Reference Application Playwright: 9 tests passed with zero failures, errors, or skips.
- Vaadin production-like pilot: 5 scenarios with zero axe, CSP, console, page, external-request, or overflow failures.
- HTMX and Reference Application RAT checks: `BUILD SUCCESS`.
- Strict OpenSpec validation, `git diff --check`, and production-isolation review: passed.

## Corrected executable outcome

The authoritative `demo.CompositeValuesPage` identifier measures 1,177 Java UTF-16 characters and 1,179 canonical encoded characters without exposing its value.
The public semantic navigation event now reaches the same canonical route and exact object context instead of `invalid-route`.
Direct load, GraphQL reconstruction, composite-value rendering, back, and forward retain the exact identity.
Malformed route shape and encoded-separator unit vectors remain bounded and non-disclosing.

## Retained warning and next step

The standalone foundation RAT goal still reports the same pre-existing `vaadin-reference/README.adoc` and generated `vaadin-reference.js` resources, neither introduced nor modified here.
The next recommended change is `add-paged-graphql-reference-autocomplete`.
