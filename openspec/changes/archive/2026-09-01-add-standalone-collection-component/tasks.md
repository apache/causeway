## 1. Public standalone collection contract

- [x] 1.1 Add the standalone collection element and host vocabulary to public contracts, exports, registration, prefix checks, and pre-upgrade declaration capture.
- [x] 1.2 Implement the property-only normalized result contract, generation-safe replacement, bounded idle, ready, empty, unsupported, and collection-state event lifecycle.
- [x] 1.3 Preserve direct-child column declarations and presentation attributes without accepting member loading, paging, sorting, filtering, activation, or associated actions.
- [x] 1.4 Add component contract and lifecycle tests for property assignment, replacement, invalid shapes, events, registration, and declaration node identity.

## 2. Native semantic result presentation

- [x] 2.1 Render authoritative object metadata through semantic object links and scalar rows through the standard value-renderer registry.
- [x] 2.2 Render supplied declared-column wrappers through shared collection cell semantics and expose unavailable cells without follow-up requests.
- [x] 2.3 Add accessible heading, description, empty, unsupported, error, responsive list and table styling with deterministic test hooks.
- [x] 2.4 Add Foundation tests for object, icon, scalar, null, hidden, disabled, error, missing-column, escaping, navigation, and native fallback behavior.

## 3. Finite Grid qualification

- [x] 3.1 Project complete standalone snapshots through existing collection Grid projection and qualification without a member range broker.
- [x] 3.2 Supply authoritative array totals and a generation-safe static range provider while omitting paging, sorting, filtering, hydration, and server range callbacks.
- [x] 3.3 Preserve resizable and reorderable presentation, small-result sizing, native fallback, adapter failure, disconnect, and replacement lifecycle.
- [x] 3.4 Extend Grid unit and browser audits for qualified, narrow, native, unsupported, superseded, and diagnostic-clean standalone results.

## 4. HTMX action-result integration

- [x] 4.1 Replace the viewer-owned collection result list with one `<cw-standalone-collection>` assigned from the unchanged semantic action result.
- [x] 4.2 Preserve application result overrides, object navigation, scalar and void handling, announcements, controller dismissal, result replacement, and route lifecycle.
- [x] 4.3 Extend HTMX JavaScript tests and Petclinic Playwright journeys for service collection results, authoritative links and icons, canonical navigation, focus, default Vaadin, and explicit native policies.

## 5. Documentation and validation

- [x] 5.1 Document standalone collection markup, property assignment, supported presentation, lifecycle events, and deliberate member-feature exclusions.
- [x] 5.2 Run complete Foundation Node and Maven suites, Grid and field browser audits, Petclinic integration and Playwright tests, IDE validation, strict OpenSpec validation, and final diff checks.
