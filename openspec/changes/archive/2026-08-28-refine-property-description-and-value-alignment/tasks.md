## 1. Label-Owned Disabled Tooltip

- [x] 1.1 Remove the standalone disabled-reason information indicator while retaining the bounded visually hidden reason.
- [x] 1.2 Attach pointer and keyboard tooltip attributes plus accessible description linkage to the property label only when a disabled reason exists.
- [x] 1.3 Replace indicator-specific baseline tooltip styles with label-owned tooltip styles without changing ordinary property-description presentation.

## 2. String Value Alignment

- [x] 2.1 Detect the standard GraphQL `String` output type and add a semantic string-value class to its read-only output.
- [x] 2.2 Add baseline and cohesive-theme logical-start alignment for string property values without changing other value types.

## 3. Verification

- [x] 3.1 Add interaction and style tests for label-owned disabled tooltips, keyboard access, standalone-indicator removal, semantic string marking, and logical-start alignment.
- [x] 3.2 Run focused and complete foundation tests, Petclinic browser acceptance, syntax and formatting checks, and strict OpenSpec validation.
