## 1. Foundation Presentation

- [x] 1.1 Make ready `<cw-unreferenced-actions>` hosts layout-transparent while preserving the generated accessible `role="group"` and ordinary connected action descendants.
- [x] 1.2 Give `.causeway-unreferenced-action-group` intrinsic bounded sizing, established action gaps, start alignment, and wrapping without panel margin or padding.
- [x] 1.3 Keep loading, empty, and error hosts non-presentational and verify that presentation changes do not alter allocation, lifecycle, ordering, focus, or interaction ownership.
- [x] 1.4 Keep the distributable JavaScript style module synchronized with the shared CSS source.

## 2. PetOwner Host Composition

- [x] 2.1 Reconcile the existing manual HTMX PetOwner edits into valid markup with `<cw-unreferenced-actions>` directly in the owner toolbar and `<cw-unreferenced-collections>` in its authored layout region.
- [x] 2.2 Preserve valid explicit actions that own custom nested result declarations while allowing eligible ordinary actions to be generated by the catch-all.
- [x] 2.3 Apply equivalent direct action-toolbar and collection catch-all composition to the Vue PetOwner page without introducing Vue-owned allocation or layout state.
- [x] 2.4 Rebuild and check in the Vue production assets required by the sample packaging workflow.

## 3. Automated Verification

- [x] 3.1 Extend foundation unreferenced-member and style tests for the ready transparent host, compact accessible group, and unchanged hidden states.
- [x] 3.2 Extend HTMX declarative-template tests to preserve valid direct toolbar placement and reject or avoid malformed nested action composition.
- [x] 3.3 Add headless HTMX browser assertions for action adjacency, bounded group geometry, narrow wrapping, no horizontal overflow, empty-state spacing, and ordinary action invocation in Vaadin and native modes.
- [x] 3.4 Add equivalent headless Vue browser assertions for generated action geometry, responsive wrapping, empty-state spacing, and interaction behavior.
- [x] 3.5 Retain regression coverage for the special explicit collection-result action and for unreferenced collection presentation.

## 4. Documentation and Validation

- [x] 4.1 Document compact toolbar use of `<cw-unreferenced-actions>` and its accessible grouping behavior in foundation and host guidance.
- [x] 4.2 Run foundation and Vue unit tests, HTMX and Vue browser suites, secured variants where affected, responsive and accessibility checks, Vue production builds, Maven packaging and RAT, IDE inspections, strict OpenSpec validation, and whitespace checks.
