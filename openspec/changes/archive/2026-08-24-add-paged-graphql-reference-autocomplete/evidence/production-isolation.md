# Production isolation verification

The change adds rich GraphQL fields and generated result types but removes or renames no existing field, argument, type, operation, route, semantic event, context method, or result kind.
Legacy `autoComplete(search)` remains executable and unchanged.
The server invokes the same Causeway property or parameter autocomplete semantic, authorization context, pending preceding arguments, and output mapper.

No dependency, Maven coordinate, npm package, lock file, Vaadin version, generated Vaadin bundle, checksum, exact CSP hash, asset URL, CDN source, telemetry, Flow, Binder, Java DataProvider, Pro component, or application-facing Vaadin element changed.
The JavaScript data-provider callback is contained within `<causeway-reference-editor>`.
Causeway contexts continue to own GraphQL, pending values, identity, validation, cancellation, events, routes, and state.

Candidate delivery remains route-lazy.
Unqualified routes make zero Vaadin requests.
Native fallback remains available through the existing viewer property and does not require the candidate bundle.
`style-src-attr 'none'` and the four exact style hashes remain unchanged.

The public window bounds browser transfer only.
Documentation and diagnostics explicitly avoid claims of database query pushdown, cursor stability, or cross-request snapshots.
Search text, protected pending arguments, identifiers, result values, and raw exception details are absent from evidence and bounded errors.
Rollback returns new clients to legacy bounded autocomplete and requires no application, route, data, or asset migration.
