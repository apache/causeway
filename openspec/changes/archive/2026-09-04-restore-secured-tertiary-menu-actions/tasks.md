## 1. Secured role seeding

- [x] 1.1 Assign the established SecMan regular-user and configuration-access roles to the deterministic HTMX user and enable the generic **Me** action.
- [x] 1.2 Apply equivalent role assignments and **Me** policy to the deterministic Vue user without broadening the Petclinic application role.
- [x] 1.3 Declare **Me**, **Configuration**, and Logout together in the shared tertiary Account menu without referencing SecMan-only services.

## 2. Verification coverage

- [x] 2.1 Extend HTMX secured integration and headless browser coverage for exact tertiary **Me**, **Configuration**, and **Sign out** actions.
- [x] 2.2 Extend Vue secured integration and headless browser coverage for the same authorized tertiary action set.
- [x] 2.3 Confirm ordinary HTMX and Vue runtimes retain their existing tertiary menu behavior.

## 3. Documentation

- [x] 3.1 Document the role-backed secured tertiary action set in both Petclinic samples and security integration guidance.

## 4. Validation

- [x] 4.1 Run focused secured HTMX and Vue tests, including headless browser acceptance.
- [x] 4.2 Run ordinary HTMX and Vue regression suites, formatting and license checks, and strict OpenSpec validation.
