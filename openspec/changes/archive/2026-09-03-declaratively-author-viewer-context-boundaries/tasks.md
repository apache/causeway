## 1. Declarative Template Contracts

- [x] 1.1 Add bounded shell and object-page template inspection with stable diagnostics for missing, duplicate, misplaced, or unresolved context boundaries and binding tokens.
- [x] 1.2 Add a closed escaped binding-token implementation for shell configuration, trusted structural slots, logical type, and object identifier.
- [x] 1.3 Add declarative default shell, landing, invalid-route, and generic object-page resources containing their complete semantic hierarchy.
- [x] 1.4 Cover cached, reload, factory, duplicate, malformed, and nested-context template cases with focused Java tests.

## 2. Slim HTMX Rendering and Routing

- [x] 2.1 Refactor `HtmxPageRenderer` to bind validated declarative templates instead of manufacturing `<cw-graphql-client>`, `<cw-object-context>`, or interaction-controller elements.
- [x] 2.2 Preserve configured endpoint, brand, widget policy, application stylesheet, authentication metadata and chrome, canonical path, CSP-compatible assets, and result slots through shell bindings.
- [x] 2.3 Preserve direct full-page, HTMX fragment, history, home resolution, semantic navigation, mutation refresh, route state, focus, announcement, and stale-response behavior.
- [x] 2.4 Update JavaScript bootstrap assumptions and diagnostics to operate on authored shell and route boundaries without creating missing semantic elements.

## 3. Application and Extension Migration

- [x] 3.1 Migrate all Petclinic page resources to declare their route page, exact route-binding context, and interaction controller while preserving presentation and paging behavior.
- [x] 3.2 Migrate Java fragment-factory fixtures and document the complete-page factory contract.
- [x] 3.3 Update HTMX usage documentation with declarative shell/page examples, reserved bindings, validation failures, and mechanical migration guidance.

## 4. Qualification

- [x] 4.1 Update controller and Petclinic integration assertions to prove one authored shell client, one authored route context, escaped canonical binding, generic fallback, and fail-closed invalid templates.
- [x] 4.2 Update browser acceptance coverage for direct and HTMX navigation, context teardown, menus, authentication, interactions, results, row previews, focus, and history.
- [x] 4.3 Run foundation Node, HTMX Java and Node, Petclinic integration and supported Playwright, JavaScript syntax, IntelliJ build, strict OpenSpec, and whitespace validation.
