## 1. Effective multiline shell state

- [ ] 1.1 Add a generated property-shell attribute derived from effective canonical, compatibility, or metadata multiline state in every render state.
- [ ] 1.2 Replace host-spelling-dependent multiline selectors with shell-state selectors while preserving wide and narrow grid placement.

## 2. Regression coverage

- [ ] 2.1 Add property component tests for canonical, legacy, and metadata-derived shell state across view, loading, error, and edit rendering.
- [ ] 2.2 Update stylesheet regression tests to require effective shell selectors and reject the incomplete legacy host selector.
- [ ] 2.3 Strengthen Petclinic geometry diagnostics and verify native narrow multiline alignment, bounded controls, and no overflow.

## 3. Validation and archive readiness

- [ ] 3.1 Run foundation tests and package verification.
- [ ] 3.2 Run focused and full Petclinic Playwright acceptance in default and native modes.
- [ ] 3.3 Run Petclinic integration, strict OpenSpec validation, compilation, license, diff, and working-tree checks.
