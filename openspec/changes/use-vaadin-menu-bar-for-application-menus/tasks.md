## 1. Baseline and qualification contract

- [x] 1.1 Record native primary, secondary, and tertiary hierarchy, labels, sections, action order, disabled reasons, keyboard, focus, dismissal, refresh, and responsive baselines.
- [x] 1.2 Record authenticated, login, empty-menu, unsupported-menu, repeated-route, and explicit-native asset-request baselines.
- [x] 1.3 Inventory existing Menu Bar foundation, HTMX, Petclinic, Reference Application, CSP, accessibility, theme, and lifecycle tests before adaptation.
- [x] 1.4 Define immutable tier, menu, section, action, and opaque identity descriptors without domain values, executable callbacks, GraphQL variables, or full errors.
- [x] 1.5 Define deterministic qualification outcomes for Vaadin tier, native tier, hidden empty tier, and family failure.
- [x] 1.6 Define family, policy, menu-generation, adapter, responsive, and connection revisions used to reject stale work.
- [x] 1.7 Record accepted hierarchy and responsive exclusions and the complete-tier fallback rule.

## 2. Deterministic Menu Bar closure

- [x] 2.1 Pin `@vaadin/menu-bar` and the exact reviewed free-core production dependency closure.
- [x] 2.2 Add an explicit browser entry point that imports only Menu Bar production modules and rejects telemetry activation.
- [x] 2.3 Add deterministic build inputs, lockfile, generated bundle, checksum, and raw plus compressed budget metadata.
- [x] 2.4 Generate complete third-party package and license metadata for the production closure.
- [x] 2.5 Add verification for pinned versions, dependency reachability, checksum, budgets, style hashes, licenses, telemetry, and prohibited runtime imports.
- [x] 2.6 Add production npm vulnerability audit and browser closure audit with zero external requests, CSP violations, axe violations, console errors, page errors, and overflow.
- [x] 2.7 Add generated Menu Bar assets and legal metadata to foundation Maven resources and RAT exclusions.
- [x] 2.8 Add ordinary Maven verification and an explicit regeneration profile without invoking npm in normal builds.
- [x] 2.9 Verify clean and incremental packaged Menu Bar bytes are identical to the accepted checksum.

## 3. Projection and qualification foundation

- [x] 3.1 Add immutable Causeway Menu Bar projection for tiers, menus, labeled and unlabeled sections, and leaf actions.
- [x] 3.2 Preserve stable tier, menu, section, service logical type, action ID, label, description, icon hint, source-order, hidden, disabled, and reason semantics.
- [x] 3.3 Assign opaque generation-scoped lookup keys that do not rely on labels and reject duplicates or malformed identities safely.
- [x] 3.4 Ensure only current enabled leaf descriptors are activatable and grouping descriptors remain non-actions.
- [x] 3.5 Add qualification for policy, family health, connection, visibility, non-empty state, current generation, representable hierarchy, and responsive width.
- [x] 3.6 Reject unsupported, malformed, stale, empty, disconnected, hidden, failed, and native-policy tiers without constructing Vaadin items.
- [x] 3.7 Add projection and qualification tests for all tier, section, action-state, identity, exclusion, and immutability cases.

## 4. Menu Bar family adapter

- [x] 4.1 Add one private Menu Bar family loader with dynamic import, definition waiting, bounded timeout, retry, and value-free diagnostics.
- [x] 4.2 Add a private adapter control that creates `vaadin-menu-bar`, applies immutable items, sets accessible tier identity, and exposes no raw API publicly.
- [x] 4.3 Translate top-level menus, nested labeled sections, unlabeled actions, disabled state, descriptions, and icon hints without changing order.
- [x] 4.4 Translate item-selected only through current opaque leaf identity and the existing Causeway activation callback.
- [x] 4.5 Ignore stale, unknown, disabled, grouping, duplicate, and malformed selected items without GraphQL, navigation, or focus side effects.
- [x] 4.6 Release item arrays, listeners, generated controls, timers, observers, and focus state exactly once.
- [x] 4.7 Fail the Menu Bar family without affecting reference, field, action, Grid, GraphQL, route, or authentication families.
- [x] 4.8 Recover connected qualified tiers from current Causeway state after an explicit family retry.
- [x] 4.9 Add adapter tests for success, timeout, definition failure, render failure, event failure, family fallback, isolation, cleanup, and recovery.

## 5. Causeway activation focus and responsive behavior

- [x] 5.1 Refactor native service-action activation into one Causeway-owned method reusable by native buttons and Menu Bar selection.
- [x] 5.2 Preserve interaction-controller preparation, invocation, cancellation, validation, result, refresh, and host navigation policy exactly once.
- [x] 5.3 Preserve bounded disabled reasons through item disabled state and accessible description.
- [x] 5.4 Track focus intent by tier and semantic menu, section, or action identity rather than toolkit nodes.
- [x] 5.5 Restore focus safely after Escape, item activation, refresh, action completion, responsive switching, native fallback, and item removal.
- [x] 5.6 Add container-responsive observation with out-of-callback state application and no GraphQL request on width transitions.
- [x] 5.7 Use Vaadin overflow at narrow widths only for accepted representative hierarchies and otherwise retain complete native tier disclosures.
- [x] 5.8 Preserve internationalized overflow labels, nested keyboard behavior, pointer behavior, visible focus, Escape dismissal, and sibling-tier isolation.
- [x] 5.9 Add focused tests for keyboard, pointer, disabled state, focus, dismissal, overflow, responsive transitions, and zero semantic rereads.

## 6. Menu host lifecycle and native fallback

- [x] 6.1 Integrate qualification into ready tier rendering without changing loading, unsupported, terminal-error, empty, hidden, or partial-diagnostic states.
- [x] 6.2 Preserve the current native renderer as explicit and automatic fallback without compatibility aliases or mixed controls.
- [x] 6.3 Gate every asynchronous completion and selection by connection, menu generation, policy, adapter, responsive, and family revisions.
- [x] 6.4 Prevent delayed imports, definitions, renders, events, and focus work from restoring disconnected, superseded, hidden, empty, or native tiers.
- [x] 6.5 Rerender every connected tier natively after one family failure from current authoritative state.
- [x] 6.6 Preserve existing menu-state and diagnostic semantic events without leaking Vaadin items, action values, callbacks, or full errors.
- [x] 6.7 Preserve composite and independently used bar elements, private and injected contexts, and generated interaction-controller ownership.
- [x] 6.8 Add host tests for every state, refresh, disconnect, replacement, policy change, responsive change, failure phase, retry, and stale callback.

## 7. HTMX stable-shell policy CSP and delivery

- [x] 7.1 Publish same-origin Menu Bar closure URL, resolved common component policy, and bounded family diagnostics through the HTMX shell.
- [x] 7.2 Extend default and deprecated editor-policy compatibility to Menu Bar while keeping deprecated pilot properties limited to their original subsets.
- [x] 7.3 Ensure explicit native policy contributes no Menu Bar style hash, request eligibility, or generated control.
- [x] 7.4 Extend exact-hash CSP metadata with the deterministic deduplicated Menu Bar candidate-originated style hash set.
- [x] 7.5 Preserve `style-src-attr 'none'`, no `unsafe-inline`, same-origin script and connection policy, and existing authentication policy.
- [x] 7.6 Lazy-load Menu Bar only after an authenticated stable shell connects a non-empty qualified tier and deduplicate across tiers and routes.
- [x] 7.7 Prove login, challenge, authentication failure, logout, empty-menu, unsupported, and native states request zero Menu Bar bytes.
- [x] 7.8 Add HTMX property, renderer, controller, CSP, policy precedence, shell publication, authentication exclusion, and packaging tests.

## 8. Themes documentation and samples

- [x] 8.1 Map established menu colors, spacing, typography, borders, radii, focus, disabled, error, and tier distinctions through Causeway CSS variables and parts.
- [x] 8.2 Preserve light, dark, reduced-motion, forced-colors, zoom, narrow, and wide rendering without inline style attributes.
- [x] 8.3 Extend the vanilla sample with deterministic qualified, native, empty, disabled, hidden, nested, labeled-section, overflow, failure, and responsive menu states.
- [x] 8.4 Extend Petclinic with representative primary, secondary, tertiary, disabled, nested, action-result, wide, narrow, default, and native journeys without raw Vaadin markup.
- [x] 8.5 Document qualification, stable-shell cost, hierarchy projection, action ownership, delivery, CSP, budgets, diagnostics, fallback, rollback, and unsupported raw APIs.
- [x] 8.6 Document exclusions for Flow, Binder, server-side state, commercial components, telemetry, CDN resources, direct GraphQL, direct navigation, and application item APIs.

## 9. Foundation security accessibility and isolation evidence

- [x] 9.1 Add source audits preventing application-facing raw Menu Bar markup, items, events, callbacks, methods, properties, Shadow DOM, direct GraphQL, or direct navigation APIs.
- [x] 9.2 Verify item objects, diagnostics, semantic events, console output, and failure details contain no domain values, protected values, GraphQL variables, prepared snapshots, full errors, or executable callbacks.
- [x] 9.3 Verify closure raw, compressed, request-count, style-hash, package-count, startup, and activation budgets with retained evidence.
- [x] 9.4 Verify zero telemetry activation, production vulnerabilities, external runtime requests, CSP violations, axe violations, console errors, page errors, overlays, clipping, and page overflow.
- [x] 9.5 Verify accessible tier, menu, section, action, expanded, disabled, reason, overflow, focus, and dismissal semantics under keyboard and pointer input.
- [x] 9.6 Verify family failure and retry remain isolated from every other adapter, GraphQL, routes, authentication, and native menu state.

## 10. Petclinic and Reference Application regression qualification

- [x] 10.1 Add deterministic Reference Application targets for all three tiers, nested and labeled sections, hidden and disabled actions, parameterized actions, object and void results, refresh, and overflow.
- [x] 10.2 Add default and native comparisons for hierarchy, source order, labels, descriptions, icon hints, action identity, disabled reasons, invocation, results, and focus.
- [x] 10.3 Add overlapping metadata, resource, preparation, refresh, import, definition, projection, and route-replacement lifecycle journeys.
- [x] 10.4 Inject family failures before connection, during import and definition, after connection, during rendering, and during event translation, then prove connected recovery.
- [x] 10.5 Verify responsive transitions and repeated routes issue no semantic reread and request the Menu Bar closure at most once.
- [x] 10.6 Verify login, authentication failure, challenge, logout, empty-menu, unsupported, explicit-native, and pilot-subset profiles request no Menu Bar closure.
- [x] 10.7 Run keyboard, pointer, Escape, focus, zoom, theme, reduced-motion, forced-colors, CSP, accessibility, clipping, order, and overflow matrices.
- [x] 10.8 Review capability inventory changes and prove clean and incremental retained resources are byte-identical.

## 11. Final qualification and evidence

- [x] 11.1 Run complete foundation Node and Web Components Maven suites.
- [x] 11.2 Run HTMX configuration, shell, CSP, route, authentication, and packaging suites.
- [x] 11.3 Run deterministic reference, field, action, Grid, and Menu Bar checksum, license, budget, telemetry, and npm audit gates.
- [x] 11.4 Run complete vanilla sample and Petclinic integration and default plus native Playwright suites.
- [x] 11.5 Run Reference Application clean package, integration, inventory, and default plus native Playwright suites.
- [x] 11.6 Run applicable RAT, production-isolation, JavaScript syntax, strict OpenSpec validation, and `git diff --check` gates.
- [x] 11.7 Record final supported modes, narrow qualification, fallback, rollback, accepted stable-shell budgets, retained evidence, and exclusions.
