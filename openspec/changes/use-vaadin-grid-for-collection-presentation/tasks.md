## 1. Baseline and qualification contract

- [ ] 1.1 Record current native table, default-row list, empty, disabled, loading, partial-error, terminal-error, paging, associated-action, focus, responsive, request, and route-disconnection behavior.
- [ ] 1.2 Select deterministic vanilla, Petclinic, and Reference Application collections covering scalar, reference, polymorphic, nullable, hidden, disabled, resource, custom, erroneous, paged, and refreshed rows.
- [ ] 1.3 Record current bounded-window operation shapes, ordering values, nullable-total states, configured maxima, request deduplication, cancellation, and materialization boundary.
- [ ] 1.4 Define the exact virtual Grid, bounded Grid, and native qualification matrix before implementing selection.
- [ ] 1.5 Define supported and excluded column, renderer, value, action, interaction, and responsive classifications.
- [ ] 1.6 Record pre-change keyboard, accessible tree, focus, wide, narrow, dark, reduced-motion, forced-colors, zoom, overflow, CSP, and external-request evidence.
- [ ] 1.7 Set the Grid gzip cap to 196608 bytes, the all-closure aggregate cap to 533504 bytes, and unaffected/native Grid delivery to zero before generating a candidate.

## 2. Deterministic Grid closure

- [ ] 2.1 Create an isolated `foundation/vaadin-grid` build with pinned npm, Node, esbuild, Playwright, axe, and free-core Vaadin Grid inputs.
- [ ] 2.2 Confirm current version-accurate Vaadin Grid imports, data-provider callback, renderer, focus, disabled, and sizing APIs from reviewed documentation and installed sources.
- [ ] 2.3 Limit production entry points to Grid and the minimum accepted free-core column support.
- [ ] 2.4 Generate one same-origin production bundle with deterministic metadata and no source-map or external runtime dependency.
- [ ] 2.5 Remove usage statistics and development-mode side effects without broadening CSP or adding runtime network access.
- [ ] 2.6 Reject Grid Pro, commercial packages, Flow, Binder, Menu Bar, telemetry, CDN, external resources, and unapproved package or runtime markers.
- [ ] 2.7 Generate complete package, version, integrity, repository, entry-point, license, copied-license, and transitive-closure metadata.
- [ ] 2.8 Capture candidate-originated style hashes under representative empty, populated, virtual, bounded, error, focus, theme, and forced-colors states.
- [ ] 2.9 Record bundle checksum, raw bytes, gzip bytes, package graph, licenses, style hashes, and production vulnerability result in reviewed policy.
- [ ] 2.10 Verify two clean candidate builds are byte-identical and remain within individual and aggregate budgets.
- [ ] 2.11 Add Maven verification, legal-resource, generated-asset, and explicit regeneration integration without invoking npm in ordinary builds.

## 3. Grid family policy and adapter foundation

- [ ] 3.1 Add `grid-widget.mjs` with bounded Vaadin/native policy, module URL, family status, configuration revision, and value-free failure detail.
- [ ] 3.2 Define the internal `cw-collection-grid` adapter without exporting raw Vaadin APIs as public application contracts.
- [ ] 3.3 Add lazy one-time module import and custom-element definition coordination for the Grid family.
- [ ] 3.4 Bind asynchronous installation to policy revision, connection generation, responsive generation, and host eligibility revision.
- [ ] 3.5 Add document-level Grid policy events so connected Causeway collections rerender immediately on configuration, failure, or recovery.
- [ ] 3.6 Ensure module, definition, renderer, and callback failure disable only Grid and produce bounded value-free diagnostics.
- [ ] 3.7 Support a later valid configuration revision recovering connected eligible collections exactly once.
- [ ] 3.8 Add adapter tests for configuration, lazy loading, success, failure, recovery, stale import, disconnect, reconnect, duplicate definition, and policy revision.

## 4. Collection qualification and presentation descriptors

- [ ] 4.1 Add immutable Grid qualification descriptors owned by `<cw-collection>` rather than the adapter.
- [ ] 4.2 Qualify only active visible collections with accepted bounded-window capability and supported semantic columns.
- [ ] 4.3 Distinguish deterministic configured ordering from encounter or unstable ordering and fail closed to native.
- [ ] 4.4 Distinguish safe zero total, safe positive total, and unavailable total without coercion.
- [ ] 4.5 Select virtual Grid only for deterministic ordering with a safe stable total.
- [ ] 4.6 Select bounded Grid only for deterministic ordering with unavailable total and normalized previous or next metadata.
- [ ] 4.7 Retain native presentation when window capability, ordering, columns, renderers, policy, family health, or lifecycle qualification fails.
- [ ] 4.8 Retain the established empty state instead of installing an empty Grid for safe-zero or terminal-empty first windows.
- [ ] 4.9 Publish bounded value-free toolkit, mode, ordering, count, responsive, and fallback diagnostics on the Causeway collection host.
- [ ] 4.10 Add exhaustive qualification tests for virtual, bounded, empty, unsupported, native, failed, narrow, and recovered outcomes.

## 5. Bounded range broker

- [ ] 5.1 Add a private Causeway-owned range API over the existing object-context `loadCollection` window operation.
- [ ] 5.2 Seed the broker from the authoritative initial active collection window without a duplicate request.
- [ ] 5.3 Validate every offset and size against non-negative safe integers, configured defaults, and server maximum.
- [ ] 5.4 Bind each request to object generation, member identity, column selection, renderer registry, policy, responsive mode, and connection lifetime.
- [ ] 5.5 Deduplicate identical cached and in-flight ranges within one accepted generation.
- [ ] 5.6 Permit bounded independently cancellable overlapping requests without reusing the existing single-load abort slot incorrectly.
- [ ] 5.7 Cap concurrent requests, cached windows, retained row snapshots, errors, and hydrated row contexts.
- [ ] 5.8 Evict and disconnect least-current cached range contexts before accepting work beyond the cap.
- [ ] 5.9 Abort or ignore obsolete ranges after refresh, route replacement, column change, width change, policy revision, member change, or disconnect.
- [ ] 5.10 Keep range-specific partial errors and metadata attached only to their accepted rows and callbacks.
- [ ] 5.11 Requalify from the first authoritative window when total, ordering, membership, version, or row selection changes.
- [ ] 5.12 Add request-count, deduplication, overlap, cancellation, eviction, partial-error, stale, disconnect, and refresh tests.

## 6. Column and semantic-cell projection

- [ ] 6.1 Preserve declarative column order, member identity, labels, test identity, visibility, and the leading object-identity column.
- [ ] 6.2 Invalidate adapter and range generations before applying added, removed, relabelled, reordered, hidden, or replaced columns.
- [ ] 6.3 Build immutable row and cell descriptors from accepted snapshots without exposing data-provider item objects to applications.
- [ ] 6.4 Render scalar, exact numeric, machine numeric, temporal, enum, Boolean, null, reference, resource, and unsupported values through existing Causeway renderer output.
- [ ] 6.5 Preserve explicit application-renderer authority even when its renderer identifier matches a standard identifier.
- [ ] 6.6 Preserve object-link navigation identity and emit the existing semantic navigation request exactly once.
- [ ] 6.7 Preserve hidden-cell absence, disabled reasons, row-relative errors, test identities, descriptions, and accessible relationships.
- [ ] 6.8 Clean every recycled cell container before projecting another row, column, renderer, error, or accessibility relationship.
- [ ] 6.9 Disable sorting, filtering, selection, editing, details, drag, resize, reorder, and other unqualified Grid affordances.
- [ ] 6.10 Fall back the whole collection when any column or renderer cannot preserve semantics rather than mixing approximate cells.
- [ ] 6.11 Add cell tests for all qualified and excluded values, application precedence, links, errors, hidden, disabled, recycling, and duplicate activation.

## 7. Grid modes paging responsiveness and focus

- [ ] 7.1 Connect Grid's lazy data provider to the range broker only in virtual mode and supply only the safe reported total.
- [ ] 7.2 Translate Grid range callbacks into bounded offsets and sizes without direct GraphQL, routing, authorization, or navigation logic.
- [ ] 7.3 Render bounded mode from only the current window without a synthetic provider total.
- [ ] 7.4 Add Causeway-owned accessible previous and next controls from normalized window metadata.
- [ ] 7.5 Preserve range labels, busy state, disabled state, last-window behavior, errors, and exact request counts across paging.
- [ ] 7.6 Retain native responsive presentation at or below the existing 48rem collection-container boundary.
- [ ] 7.7 Use a container-aware observer to switch wide and narrow modes without user-agent detection or page overflow.
- [ ] 7.8 Reuse current authoritative rows when presentation mode changes and request no range solely because cached current data is already sufficient.
- [ ] 7.9 Record focus identity by collection, domain row, column, and semantic role rather than recycled DOM node.
- [ ] 7.10 Restore focus only when route, collection, row, column, visibility, usability, and presentation remain current.
- [ ] 7.11 Preserve associated-action order, visibility, usability, prompting, invocation, navigation, and focus outside Grid.
- [ ] 7.12 Add virtual, bounded, pager, terminal, narrow, repeated-resize, zoom, focus, associated-action, and no-overflow tests.

## 8. Collection host lifecycle and native fallback

- [ ] 8.1 Integrate Grid selection into ready collection rendering without changing activation, metadata loading, loading, empty, disabled, partial-error, terminal-error, or hidden states.
- [ ] 8.2 Preserve native table and default-row renderers as explicit and automatic fallback implementations.
- [ ] 8.3 Disconnect Grid callbacks, observers, listeners, cache entries, and row contexts exactly once on host disconnect or member replacement.
- [ ] 8.4 Prevent delayed Grid work from restoring a hidden, narrow, disconnected, superseded, or native-policy collection.
- [ ] 8.5 Rerender all connected qualified collections natively after one Grid family failure while preserving current authoritative state.
- [ ] 8.6 Prevent family fallback from affecting references, fields, actions, menus, GraphQL, routes, or sibling collections.
- [ ] 8.7 Preserve semantic collection-state publication without leaking Vaadin items, callbacks, values, or internal errors.
- [ ] 8.8 Add host tests for every established state, fallback phase, hidden transition, route replacement, policy switch, failure, recovery, and stale callback.

## 9. HTMX policy CSP and route-lazy delivery

- [ ] 9.1 Serve the Grid closure and policy metadata as same-origin packaged resources.
- [ ] 9.2 Extend default and explicit deprecated-editor Vaadin CSP to the deterministic deduplicated union including Grid hashes.
- [ ] 9.3 Keep explicit native and former pilot-subset CSP free of Grid hashes and request eligibility.
- [ ] 9.4 Retain `style-src-attr 'none'`, same-origin scripts and connections, and no `unsafe-inline` or external source in every policy mode.
- [ ] 9.5 Publish shell diagnostics for Grid eligibility, effective source, family health, closure URL, and bounded policy revision without domain values.
- [ ] 9.6 Configure Grid only under default, explicit component Vaadin, or explicit deprecated-editor Vaadin complete policy.
- [ ] 9.7 Request Grid only after the first connected active wide collection qualifies, and reuse the closure for later collections.
- [ ] 9.8 Assert narrow, inactive, unqualified, unaffected, pilot-subset, and native routes request no Grid and never wait for it.
- [ ] 9.9 Add controller, properties, shell, CSP, asset, route-fragment, policy-precedence, lazy-request, family-isolation, and invalid-policy tests.

## 10. Themes samples and documentation

- [ ] 10.1 Map documented `--causeway-*` variables into internal Grid dimensions, cells, headers, focus, errors, and paging without application-facing Vaadin selectors.
- [ ] 10.2 Verify labels, descriptions, disabled reasons, busy state, empty state, errors, rows, cells, links, paging, associated actions, and focus in baseline and cohesive themes.
- [ ] 10.3 Extend the vanilla sample with deterministic virtual, bounded, empty, disabled, erroneous, excluded, failure, narrow, and native Grid presentations.
- [ ] 10.4 Update Petclinic pages and fixtures to demonstrate wide Grid and narrow native collection presentation without raw Vaadin markup.
- [ ] 10.5 Document Grid qualification, virtual and bounded modes, totals, ordering, native fallback, responsiveness, range limits, route loading, closure, budget, CSP, and dependency-update obligations.
- [ ] 10.6 Document that pushdown, sorting, filtering, selection, editing, resizing, reordering, details, Grid Pro, Flow, Binder, telemetry, CDN, and raw Grid APIs remain excluded.
- [ ] 10.7 Update the Vaadin presentation roadmap and planned-change index to record the archived field/action tranche and active Grid tranche.
- [ ] 10.8 Add source and bundle audits preventing raw application-facing Grid APIs, unapproved affordances, and accidental persistence-pushdown claims.

## 11. Petclinic and Reference Application qualification

- [ ] 11.1 Add default and explicit-native Petclinic assertions for wide Grid, narrow native, columns, values, links, paging, associated actions, routes, focus, and requested assets.
- [ ] 11.2 Select deterministic retained Reference Application targets for stable total, unavailable total, unstable ordering, scalar, reference, polymorphic, nullable, hidden, disabled, resource, custom, error, and empty classifications.
- [ ] 11.3 Select deterministic collection-associated action and authoritative membership-change targets.
- [ ] 11.4 Add default and native collection journeys comparing rows, values, totals, ordering, labels, descriptions, errors, navigation, paging, actions, focus, and outcomes.
- [ ] 11.5 Trigger overlapping and repeated virtual ranges and verify bounded requests, deduplication, cache limits, ordering, totals, errors, and current generation.
- [ ] 11.6 Exercise bounded Grid previous and next windows without inventing total or probing beyond terminal metadata.
- [ ] 11.7 Mutate membership or ordering, refresh, and verify stale ranges, totals, contexts, cells, and focus cannot mix with current data.
- [ ] 11.8 Inject Grid failures before connection, after connection, during rendering, and during data callbacks and verify independent native recovery.
- [ ] 11.9 Replace routes and switch widths and policies during import, definition, render, and range work and verify disconnected work cannot alter current state.
- [ ] 11.10 Exercise default, explicit component, deprecated editor, former pilot, conflicting, invalid, and native policy profiles with diagnostics, hashes, requests, and precedence.
- [ ] 11.11 Verify clean and incremental capability inventory and retained target generation remain byte-identical unless reviewed evidence requires a baseline update.

## 12. Security accessibility and performance evidence

- [ ] 12.1 Measure Grid, route-level, and all-closure raw and gzip assets against accepted budgets.
- [ ] 12.2 Run production npm audit and retain reviewed vulnerability results for every packaged closure.
- [ ] 12.3 Capture and verify candidate-originated style hashes across virtual, bounded, empty, error, focus, theme, and forced-colors states.
- [ ] 12.4 Run enforcing exact-hash CSP with `style-src-attr 'none'` across default, native, deprecated-editor, and former-pilot modes.
- [ ] 12.5 Run axe, keyboard, pointer, zoom, labels, descriptions, disabled-reason, busy, empty, error, visible-focus, paging, responsive, theme, reduced-motion, and forced-colors checks.
- [ ] 12.6 Assert zero unexpected console errors, page errors, external requests, stale cells, duplicate controls, callback leaks, clipping, and page overflow.
- [ ] 12.7 Verify row and protected values remain absent from failure events, diagnostics, operation summaries, screenshots, request metadata, and route evidence.
- [ ] 12.8 Verify no Flow, Binder, Grid Pro, commercial code, Menu Bar, telemetry, CDN, server-side Vaadin runtime, or unapproved control enters production closure.
- [ ] 12.9 Retain checksums, legal manifests, policy reports, browser output, request evidence, value-free screenshots, and reproduction commands.

## 13. Final qualification

- [ ] 13.1 Run complete foundation Node and web-component Maven suites.
- [ ] 13.2 Run HTMX configuration, shell, CSP, route, security, and packaging suites.
- [ ] 13.3 Run deterministic reference, field, action, and Grid checksum, license, budget, telemetry, and npm audit gates.
- [ ] 13.4 Run complete vanilla sample and Petclinic integration and default plus native Playwright suites.
- [ ] 13.5 Run Reference Application clean package, integration, inventory, and default plus native Playwright suites.
- [ ] 13.6 Run strict-CSP, accessibility, keyboard, pointer, zoom, responsive, theme, reduced-motion, forced-colors, route-isolation, and failure matrices.
- [ ] 13.7 Run applicable RAT checks, production-isolation review, JavaScript syntax, strict OpenSpec validation, and `git diff --check`.
- [ ] 13.8 Record final gate results, supported modes, fallback behavior, rollback, accepted budgets, retained evidence, and remaining exclusions.
