## 1. Secured role seeding

- [ ] 1.1 Assign the established SecMan regular-user and configuration-access roles to the deterministic HTMX user.
- [ ] 1.2 Apply equivalent role assignments to the deterministic Vue user without broadening the Petclinic application role.

## 2. Verification coverage

- [ ] 2.1 Extend HTMX secured integration and headless browser coverage for exact tertiary **Me**, **Configuration**, and **Sign out** actions.
- [ ] 2.2 Extend Vue secured integration and headless browser coverage for the same authorized tertiary action set.
- [ ] 2.3 Confirm ordinary HTMX and Vue runtimes retain their existing tertiary menu behavior.

## 3. Documentation

- [ ] 3.1 Document the role-backed secured tertiary action set in both Petclinic samples and security integration guidance.

## 4. Validation

- [ ] 4.1 Run focused secured HTMX and Vue tests, including headless browser acceptance.
- [ ] 4.2 Run ordinary HTMX and Vue regression suites, formatting and license checks, and strict OpenSpec validation.
