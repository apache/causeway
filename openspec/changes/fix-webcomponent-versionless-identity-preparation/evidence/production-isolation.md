# Production isolation verification

No Maven POM, npm package manifest, lock file, dependency version, or third-party source changed.
No public GraphQL query, mutation, type, field, argument, scalar, input, output, or generated-name contract changed.
The implementation consumes additional concrete value types through the established one-type targeted-introspection mechanism.

No public `<causeway-*>` element, context method, semantic event name, event detail, result kind, canonical route, browser history policy, HTMX lifecycle, or asset URL changed.
No Vaadin input, generated bundle, checksum, exact CSP hash, qualification policy, route-lazy trigger, native fallback, or default-selection policy changed.

The correction changes only internal field selection for concrete object metadata and bounded rejection of abstract row selection.
Rollback requires no schema, persisted-data, route, application configuration, or asset migration.
