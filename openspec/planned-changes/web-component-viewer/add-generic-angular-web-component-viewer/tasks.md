## 1. Package and Router Contract

- [ ] 1.1 Verify all accepted GraphQL, application-entry, composite-object, menu-bar, and declarative context-boundary prerequisites are complete.
- [ ] 1.2 Establish the optional Angular package, peer dependencies, providers, custom-element schema policy, default theme, and acceptance application.
- [ ] 1.3 Implement canonical route records, parsing, configurable base path, bookmark resolution, and route-input binding using Angular Router.
- [ ] 1.4 Maintain cross-viewer route fixtures shared with HTMX, Vue, and Svelte implementations.

## 2. Custom and Generic Route Pages

- [ ] 2.1 Implement exact-logical-type registration for standalone components and lazy `loadComponent` loaders.
- [ ] 2.2 Implement application-authored custom and generic route templates that declare one `<cw-object-context>`, with `<cw-object>` in the generic fallback.
- [ ] 2.3 Bind canonical route identity into declared contexts and fail closed diagnostically when a page omits or duplicates its route context.
- [ ] 2.4 Key route lifecycle by canonical bookmark identity and dispose disconnected requirements and superseded loaders deterministically.
- [ ] 2.5 Verify `<cw-object>` never reads Angular Router, injector, or page-registry state.

## 3. Shell, Events, and Policy

- [ ] 3.1 Implement the application-authored stable Angular shell declaring one `<cw-graphql-client>`, `<cw-menubars>`, `router-outlet`, endpoint binding, and route lifecycle states.
- [ ] 3.2 Configure `CUSTOM_ELEMENTS_SCHEMA` and preserve custom-element attributes, properties, native events, and upgrade behavior.
- [ ] 3.3 Bridge semantic navigation and result events into replaceable Angular Router and result policy.
- [ ] 3.4 Implement configurable home-page object and service-action policy without duplicating domain state in Angular services or signals.

## 4. Verification and Documentation

- [ ] 4.1 Test deep links, refresh, history, custom pages, lazy-loader races, generic fallback, menus, interactions, results, partial errors, absent objects, and access denial.
- [ ] 4.2 Test declarative client/context ownership, missing and duplicate context diagnostics, custom-element attributes, properties, native events, upgrade ordering, route reuse, and disconnect.
- [ ] 4.3 Verify responsive layout, keyboard navigation, focus after routing, announcements, light/dark themes, and accessibility.
- [ ] 4.4 Document installation, providers, route grammar, custom pages, fallback, policy, custom-element schemas, client-rendered scope, theming, lifecycle, and framework-neutral boundaries.
- [ ] 4.5 Run Angular, browser, Maven, GraphQL, accessibility, formatting, and strict OpenSpec validation checks.
