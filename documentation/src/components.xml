<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE section SYSTEM "./documentation.dtd">
<section>
  <title>Components &amp; interfaces</title>

  <para>The following interfaces are defined in the NOF and can be
  implemented:-</para>

  <list>
    <el>Configuration</el>

    <el></el>

    <el>NakedObjects - repository (abstract singleton - a class)</el>

    <el></el>

    <el>NakedObjectLoader</el>

    <el>NakedObjectPersistor</el>

    <el></el>

    <el>NakedObjectSpecificationLoader</el>

    <el>NakedObjectSpecification</el>

    <el>ActionPeer</el>

    <el>OneToOnePeer</el>

    <el>OneToManyPeer</el>

    <el></el>

    <el>Session</el>
  </list>

  <para>The following are second-class interfaces, used to extend the core
  components above:-</para>

  <list>
    <el>NakedObjectStore</el>

    <el>HelpManager</el>

    <el>AuthorisationManager</el>
  </list>

  <para></para>

  <list>
    <el>ProxyObjectManager</el>

    <el>ProxyPeerFactory</el>

    <el>ProxyAction</el>

    <el>ProxyOneToOne</el>

    <el>ProxyOneToMany</el>

    <el></el>

    <el>SecurityPeerFactory</el>

    <el>AuthorisationManager</el>

    <el>ActionAuthorisation</el>

    <el>OneToOneAuthorisation</el>

    <el>OneToManyAuthorisation</el>

    <el></el>

    <el>HelpPeerFactory</el>

    <el>HelpManager</el>

    <el>ActionHelp</el>

    <el>OneToOneHelp</el>

    <el>OneToManyHelp</el>

    <el></el>

    <el>LocalObjectManager</el>

    <el>TransactionPeerFactory</el>

    <el>ActionTransaction</el>

    <el>OneToOneTransaction</el>

    <el>OneToManyTransaction</el>

    <el></el>

    <el>ObjectLoaderImpl</el>

    <el>PojoAdapterMap</el>

    <el>IdentityAdapterMap</el>

    <el></el>
  </list>
</section>