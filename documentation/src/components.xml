<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE section SYSTEM "./documentation.dtd">
<section>
  <title>Components &amp; interfaces</title>

  <para>The following interfaces are defined in the NOF and can be
  implemented:-</para>

  <list>
    <el>NakedObjectComponent</el>

    <el>DebugInfo</el>

    <el>AboutNakedObjects</el>

    <el>SplashWindow</el>

    <el></el>

    <el></el>

    <el>NakedObjectConfiguration</el>

    <item>
      <para></para>
    </item>

    <el>Adapters</el>

    <el>NakedObjects - repository (abstract singleton - a class)</el>

    <el></el>

    <el>NakedValue</el>

    <el>NakedReference</el>

    <el>NakedObject</el>

    <el>NakedCollection</el>

    <el></el>

    <el>ResolveState</el>

    <el>Persistable</el>

    <el>Version</el>

    <el>Oid -&gt; SerialOid</el>

    <el></el>

    <el>NakedObjectLoader</el>

    <el>NakedObjectPersistor</el>

    <el>-- DirtyObjectSet</el>

    <el>-- InstancesCriteria -&gt; PatternCriteria, PatternObjectCriteria,
    TitleCriteria</el>

    <el></el>

    <el>Hint</el>

    <el>Consent</el>

    <el>-- Allow</el>

    <el>-- Veto</el>

    <el></el>

    <el>NakedObjectSpecificationLoader</el>

    <el>NakedObjectSpecification</el>

    <el>ActionPeer</el>

    <el>OneToOnePeer</el>

    <el>OneToManyPeer</el>

    <el>MemberIdentifier</el>

    <el></el>

    <el>Session</el>

    <el></el>

    <el>ObjectViewingMechanismListener -&gt; SystemExitShutdownListener</el>

    <el>UserFeedback</el>
  </list>

  <para>The following are second-class interfaces, used to extend the core
  components above:-</para>

  <list></list>

  <para></para>

  <list>
    <el>SkylarkViewer (implements NakedObjectComponent)</el>

    <el>AcceptanceTestCase -&gt; JavaAcceptanceTestCase</el>

    <el>.. what about all the XAT classes?</el>

    <el></el>

    <el>PrimitiveReflector?</el>

    <el>InternalReflector? - how are these to be replaced by
    NOSpecLoader?</el>

    <el></el>

    <el>Fixture</el>

    <el>FixtureBuilder</el>

    <el></el>

    <el>LocalObjectManager</el>

    <el>-- Transaction</el>

    <el>-- PersistenceCommand</el>

    <el>---- CreateObjectCommand</el>

    <el>---- DestroyObjectCommand</el>

    <el>---- SaveObjectCommand</el>

    <el></el>

    <el>-- NakedObjectStore</el>

    <el>---- TransientObjectStore</el>

    <el>---- MemoryObjectStore</el>

    <el>---- XMLObjectStore</el>

    <el>-- PersistAlgorithm - DefaultPersistAlgorithm</el>

    <el>---- OidGenerator - SimpleOidGenerator, TimeBasedOidGenerator</el>

    <el></el>

    <el>ProxyObjectManager</el>

    <el></el>

    <el>ProxyPeerFactory</el>

    <el>ProxyAction</el>

    <el>ProxyOneToOne</el>

    <el>ProxyOneToMany</el>

    <el></el>

    <el>Distribution</el>

    <el>-- ServerDistribution</el>

    <el>-- CommandClient</el>

    <el>-- Data -&gt; ValueData, ObjectData, CollectionData, NullData</el>

    <el>-- DataFactory</el>

    <el>-- DataHelper</el>

    <el>SingleResponseUpdateNotifier</el>

    <el></el>

    <el>SecurityPeerFactory</el>

    <el>AuthorisationManager</el>

    <el>ActionAuthorisation</el>

    <el>OneToOneAuthorisation</el>

    <el>OneToManyAuthorisation</el>

    <el></el>

    <el>HelpPeerFactory</el>

    <el>HelpManager</el>

    <el>ActionHelp</el>

    <el>OneToOneHelp</el>

    <el>OneToManyHelp</el>

    <el></el>

    <el>TransactionalPeerFactory</el>

    <el>ActionTransaction</el>

    <el>OneToOneTransaction</el>

    <el>OneToManyTransaction</el>

    <el></el>

    <el>ObjectLoaderImpl</el>

    <el>-- AdapterFactory</el>

    <el>-- ObjectFactory</el>

    <el>-- PojoAdapterMap - PojoAdapterHashMap</el>

    <el>-- IdentityAdapterMap - IdentityAdapterHashMap</el>

    <el></el>

    <el></el>

    <el></el>

    <el>JavaBusinessObjectContainer</el>

    <el>JavaObjectFactory</el>

    <el>JavaSpecificationLoader</el>

    <el>JavaAdapterFactory</el>

    <el>JavaFixtureBuilder</el>

    <el></el>
  </list>

  <para>Java Applications</para>

  <list>
    <el>BusinessObjectContainer</el>

    <el>Lookup</el>

    <el>NonPersistable</el>

    <el>ApplicationException</el>

    <el>InternalCollection</el>

    <el>FieldAbout</el>

    <el>ActionAbout</el>

    <el>Validity</el>

    <el>Clock</el>

    <el>BusinessValue -&gt; Color, Date, PhoneNumber etc</el>

    <el>BusinessValueHolder - &gt; TextString, FloatingPointNumber, Logical,
    Password etc</el>
  </list>
</section>