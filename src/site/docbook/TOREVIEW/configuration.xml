<!--
  Licensed to the Apache Software Foundation (ASF) under one
  or more contributor license agreements.  See the NOTICE file
  distributed with this work for additional information
  regarding copyright ownership.  The ASF licenses this file
  to you under the Apache License, Version 2.0 (the
  "License"); you may not use this file except in compliance
  with the License.  You may obtain a copy of the License at

        http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing,
  software distributed under the License is distributed on an
  "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
  KIND, either express or implied.  See the License for the
  specific language governing permissions and limitations
  under the License.
-->

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE article PUBLIC "-//OASIS//DTD Simplified DocBook XML V1.1//EN"
"http://www.oasis-open.org/docbook/xml/simple/1.1/sdocbook.dtd">
<article>
  <articleinfo>
    <title>Configuration</title>

    <author>
      <firstname></firstname>

      <surname></surname>

      <affiliation>
        <orgname></orgname>
      </affiliation>
    </author>

    <pubdate></pubdate>
  </articleinfo>

  <section>
    <title>Configuration</title>

    <section>
      <title>Configuration Settings</title>

      <para>The configuration file (<filename class="directory"
      moreinfo="none">isis.properties</filename>) needs to specify
      what resources are used by the application and what fixtures to load.
      These details are common to all the modes that the NOF can be run in.
      Additional properties can be also specified for use in specific modes,
      such as database connection details when using the SQL object
      store.</para>

      <para>The configuration file <filename class="directory"
      moreinfo="none">isis.properties</filename> is always picked up.
      In addition other properties files will be picked up, if present, based
      on the type, viewer, persistor and connection command line switches with
      the filename matching the option. E.g.</para>

      <para>-r (or --persistor) hibernate will cause <filename
      moreinfo="none">persistor.properties</filename> and <filename
      class="directory"
      moreinfo="none">persistor_hibernate.properties</filename> to be
      used</para>

      <para>-t (or --type) client will cause <filename class="directory"
      moreinfo="none">client.properties</filename> to be used</para>

      <para>-v (or --viewer) dnd will cause <filename
      moreinfo="none">viewer.properties</filename> and <filename
      class="directory" moreinfo="none">viewer_dnd.properties</filename> to be
      used</para>

      <para>-x (or -- connector) xstream-sockets will cause <filename
      moreinfo="none">transport.properties</filename>, <filename
      moreinfo="none">transport_sockets.properties</filename>, <filename
      moreinfo="none">protocol.properties</filename> and <filename
      moreinfo="none">protocol_xstream.properties</filename> to be loaded;
      because of the way that connector is implemented it will also load
      <literal>persistor.properties</literal> and <filename class="directory"
      moreinfo="none">persistor_xstream-sockets.properties</filename>
      too.</para>

      <para>In addition a particular file can be selected with the -c switch.
      For example</para>

      <para>-c <filename class="directory"
      moreinfo="none">mysettings.properties</filename></para>

      <section>
        <title>Properties file contents</title>

        <para>Each properties file can contain lists of services and fixtures
        as well as any specific settings (e.g. Hibernate setting). Services
        are defined using the <literal>isis.services</literal>
        property, fixtures by the <literal>isis.fixtures</literal>
        property.</para>

        <para>As a shortcut you can specify the prefix for all classes listed
        in the <literal>services</literal> property using
        <literal>services.prefix</literal> and the <literal>fixtures</literal>
        property using <literal>fixtures.prefix</literal>.</para>

        <programlisting format="linespecific"># uncomment to hide splash
#isis.nosplash
isis.locale=en_GB
isis.services.prefix = org.apache.isis.example.expenses
isis.services = resources.ExpenseTypeFactory \
                       , resources.naive.NaiveClaimRepository\
                       , resources.naive.NaiveEmployeeRepository\
                       , resources.naive.NaiveExpenseTypeRepository\
                       , resources.naive.NaiveItemRepository

isis.fixtures.prefix=org.apache.isis.example.expenses.fixture
isis.fixtures=ExpenseTypes, Employees, ExampleClaim, Context</programlisting>

        <para>Other settings that can be set include</para>

        <itemizedlist>
          <listitem>
            <para>isis.nosplash</para>
          </listitem>

          <listitem>
            <para>isis.locale (e.g. en_GB)</para>
          </listitem>
        </itemizedlist>

        <para>See the relevant section for details of settings specific to
        persistence, web viewer etc.</para>

        <para>The <filename class="directory"
        moreinfo="none">logging.properties</filename> file allows control over
        the logging settings. The NOF uses log4j for logging so please refer
        to <ulink
        url="http://logging.apache.org/log4j/docs/documentation.html">Apache</ulink>
        log4j documentation.</para>
      </section>
    </section>

    <section>
      <title>Reflector Properties</title>

      <section>
        <title>Value formats</title>

        <section>
          <title>Byte format</title>

          <para>The format for all bytes can be set, replacing the default
          format derived from the system, using the following property to
          specify a mask:</para>

          <programlisting format="linespecific">isis.value.format.byte=####</programlisting>

          <para>The mask is used to set up a
          <literal>java.text.DecimalFormat</literal> formatting object so
          details of the mask format can be found in the Java
          documentation.</para>

          <para>This setting can be overriden for a specific field using the
          <ulink
          url="recognised-annotations.html#mask-annotation"><literal>@Mask</literal>
          annotation</ulink>.</para>
        </section>

        <section>
          <title>Date Format</title>

          <para>The format for all dates can be set, replacing the default
          format derived from the system, using the following property to
          specify one of <emphasis>long</emphasis>,
          <emphasis>medium</emphasis>, <emphasis>short</emphasis>,
          <emphasis>isolong</emphasis>, <emphasis>isoshort</emphasis> or a
          mask:</para>

          <programlisting format="linespecific">isis.value.format.date=dd/MM/yy</programlisting>

          <para>When a mask is specified it is used to set up a
          <literal>java.text.SimpleDateFormat</literal> formatting object so
          details of the mask format can be found in the Java
          documentation.</para>

          <para>This setting can be overriden for a specific field using the
          <ulink
          url="recognised-annotations.html#mask-annotation"><literal>@Mask</literal>
          annotation</ulink>.</para>
        </section>

        <section>
          <title>Date/time Format</title>

          <para>The format for all date/time values can be set, replacing the
          default format derived from the system, using the following property
          to specify one of <emphasis>long</emphasis>,
          <emphasis>medium</emphasis>, <emphasis>short</emphasis>,
          <emphasis>isolong</emphasis>, <emphasis>isoshort</emphasis> or a
          mask:</para>

          <programlisting format="linespecific">isis.value.format.datetime=dd/MM/yy</programlisting>

          <para>When a mask is specified it is used to set up a
          <literal>java.text.SimpleDateFormat</literal> formatting object so
          details of the mask format can be found in the Java
          documentation.</para>

          <para>This setting can be overriden for a specific field using the
          <ulink
          url="recognised-annotations.html#mask-annotation"><literal>@Mask</literal>
          annotation</ulink>.</para>
        </section>

        <section>
          <title>Decimal format</title>

          <para>The format for <literal>Bigdecimal</literal> values can be
          set, replacing the default format derived from the system, using the
          following property to specify a mask:</para>

          <programlisting format="linespecific">isis.value.format.decimal=####</programlisting>

          <para>The mask is used to set up a
          <literal>java.text.DecimalFormat</literal> formatting object so
          details of the mask format can be found in the Java
          documentation.</para>

          <para>This setting can be overriden for a specific field using the
          <ulink
          url="recognised-annotations.html#mask-annotation"><literal>@Mask</literal>
          annotation</ulink>.</para>
        </section>

        <section>
          <title>Double format</title>

          <para>The format for all double values can be set, replacing the
          default format derived from the system, using the following property
          to specify a mask:</para>

          <programlisting format="linespecific">isis.value.format.double=####</programlisting>

          <para>The mask is used to set up a
          <literal>java.text.DecimalFormat</literal> formatting object so
          details of the mask format can be found in the Java
          documentation.</para>

          <para>This setting can be overriden for a specific field using the
          <ulink
          url="recognised-annotations.html#mask-annotation"><literal>@Mask</literal>
          annotation</ulink>.</para>
        </section>

        <section>
          <title>Float format</title>

          <para>The format for all float values can be set, replacing the
          default format derived from the system, using the following property
          to specify a mask:</para>

          <programlisting format="linespecific">isis.value.format.float=####</programlisting>

          <para>The mask is used to set up a
          <literal>java.text.DecimalFormat</literal> formatting object so
          details of the mask format can be found in the Java
          documentation.</para>

          <para>This setting can be overriden for a specific field using the
          <ulink
          url="recognised-annotations.html#mask-annotation"><literal>@Mask</literal>
          annotation</ulink>.</para>
        </section>

        <section>
          <title>Integer format</title>

          <para>The format for all integers (including
          <literal>BigInteger</literal>) can be set, replacing the default
          format derived from the system, using the following property to
          specify a mask:</para>

          <programlisting format="linespecific">isis.value.format.int=####</programlisting>

          <para>The mask is used to set up a
          <literal>java.text.DecimalFormat</literal> formatting object so
          details of the mask format can be found in the Java
          documentation.</para>

          <para>This setting can be overriden for a specific field using the
          <ulink
          url="recognised-annotation.html#mask-annotation"><literal>@Mask</literal>
          annotation</ulink>.</para>
        </section>

        <section>
          <title>Long format</title>

          <para>The format for all long values can be set, replacing the
          default format derived from the system, using the following property
          to specify a mask:</para>

          <programlisting format="linespecific">isis.value.format.long=####</programlisting>

          <para>The mask is used to set up a
          <literal>java.text.DecimalFormat</literal> formatting object so
          details of the mask format can be found in the Java
          documentation.</para>

          <para>This setting can be overriden for a specific field using the
          <ulink
          url="recognised-annotations.html#mask-annotation"><literal>@Mask</literal>
          annotation</ulink>.</para>
        </section>

        <section>
          <title>Short format</title>

          <para>The format for all short values can be set, replacing the
          default format derived from the system, using the following property
          to specify a mask:</para>

          <programlisting format="linespecific">isis.value.format.short=####</programlisting>

          <para>The mask is used to set up a
          <literal>java.text.DecimalFormat</literal> formatting object so
          details of the mask format can be found in the Java
          documentation.</para>

          <para>This setting can be overriden for a specific field using the
          <ulink
          url="recognised-annotations.html#mask-annotation"><literal>@Mask</literal>
          annotation</ulink>.</para>
        </section>

        <section>
          <title>Time Format</title>

          <para>The format for all time values can be set, replacing the
          default format derived from the system, using the following property
          to specify one of <emphasis>long</emphasis>,
          <emphasis>medium</emphasis>, <emphasis>short</emphasis>,
          <emphasis>isolong</emphasis>, <emphasis>isoshort</emphasis> or a
          mask:</para>

          <programlisting format="linespecific">isis.value.format.time=ddMMyyyy hhmm</programlisting>

          <para>When a mask is specified it is used to set up a
          <literal>java.text.SimpleDateFormat</literal> formatting object so
          details of the mask format can be found in the Java
          documentation.</para>

          <para>This setting can be overriden for a specific field using the
          <ulink
          url="recognised-annotations.html#mask-annotation"><literal>@Mask</literal>
          annotation</ulink>.</para>
        </section>

        <section>
          <title>Timestamp Format</title>

          <para>The format for time stamp values can be set, replacing the
          default format derived from the system, using the following property
          to specify one of <emphasis>long</emphasis>,
          <emphasis>medium</emphasis>, <emphasis>short</emphasis>,
          <emphasis>isolong</emphasis>, <emphasis>isoshort</emphasis> or a
          mask:</para>

          <programlisting format="linespecific">isis.value.format.timestamp=hh:mm</programlisting>

          <para>When a mask is specified it is used to set up a
          <literal>java.text.SimpleDateFormat</literal> formatting object so
          details of the mask format can be found in the Java
          documentation.</para>

          <para>This setting can be overriden for a specific field using the
          <ulink
          url="recognised-annotations.html#mask-annotation"><literal>@Mask</literal>
          annotation</ulink>.</para>
        </section>
      </section>

      <section>
        <title>Reflector facet decorators</title>

        <para>The reflector facet-decorators property specifies a list of
        <literal>FacetDecoratorInstaller</literal> objects that should be
        installed and registered with the reflector. These decorators decorate
        specific facets of the metamodel, allowing it change or modify their
        behaviour. This allows us to add internationalization, help look up
        and other features. Although transaction management is also achieved
        by decorating the reflector this is done automatically and does not
        need to be specified using this property. The following example adds a
        single decorator that provide internationalization via resource
        files.</para>

        <programlisting format="linespecific">isis.reflector.facet-decorators=resource-i18n</programlisting>
      </section>
    </section>

    <section>
      <title>Exploration Properties</title>

      <section>
        <title>Users</title>

        <para>The list of users that can be switched between during
        exploration can be listed, separated by commas, with the following
        property. If no users are specified the default user "exploration"
        will be used and switching between users will not be possible.</para>

        <programlisting format="linespecific">isis.exploration.users=sven, dick, bob</programlisting>
      </section>

      <section>
        <title>Exploration options</title>

        <para>To disable the showing of exploration menu items set the
        following property to false (by default exploration options are
        shown).</para>

        <programlisting format="linespecific">isis.exploration.show=false</programlisting>
      </section>
    </section>

    <section>
      <title>Internationalization resource files</title>

      <section>
        <title id="i18n-files">Files</title>

        <para>Translated names, descriptions and help text for a specific
        language should be held in a file named in the following
        format:</para>

        <literallayout>i18n_&lt;language code&gt;_&lt;country code&gt;.properties</literallayout>

        <para>The language and country codes must reflect the translated
        language are ISO standards and are detailed in the <link
        linkend="locale">Locale section</link>.</para>

        <para>Theses files must be on the root of the class path, otherwise
        they cannot be loaded.</para>
      </section>

      <section>
        <title>Properties</title>

        <para>Each property can have a line for each of the name, description
        and help text. The format for each is shown below.</para>

        <programlisting format="linespecific">&lt;fully qualified class name&gt;.property.&lt;property name&gt;.name=&lt;translated name&gt;
&lt;fully qualified class name&gt;.property.&lt;property name&gt;.description=&lt;translated description&gt;
&lt;fully qualified class name&gt;.property.&lt;property name&gt;.help=&lt;translated help&gt;</programlisting>

        <para>The class name must be fully qualified and the property name is
        the short name provided by the reflector. All the values are case
        sensitive. The following shows the translated name and description
        being specified for the property <emphasis>fullName</emphasis>, which
        is defined by the <literal>getFullName</literal> method.</para>

        <programlisting format="linespecific">example.dom.Contact.property.fullName.name=Nom
example.dom.Contact.property.fullName.description=Le nom complet du client</programlisting>
      </section>

      <section>
        <title>Collections</title>

        <para><emphasis>TODO: presumably i18n is supported for collections,
        too?</emphasis></para>
      </section>

      <section>
        <title>Actions</title>

        <para>Actions are specified in a similar fashion to properties but
        with the keyword <emphasis>action</emphasis> instead of
        <emphasis>property</emphasis>. Parameters within an action can also be
        translated by inserting <emphasis>parameter</emphasis> and a number
        before the keyword. Each parameter must be numbered to show its
        position, starting from one (1), eg <emphasis>parameter1</emphasis>,
        <emphasis>parameter2</emphasis> etc.</para>

        <programlisting format="linespecific">&lt;fully qualified class name&gt;.action.&lt;action name&gt;.name=&lt;translated name&gt;
&lt;fully qualified class name&gt;.action.&lt;action name&gt;.description=&lt;translated description&gt;
&lt;fully qualified class name&gt;.action.&lt;action name&gt;.help=&lt;translated help&gt;

&lt;fully qualified class name&gt;.action.&lt;action name&gt;.parameter&lt;index&gt;.name=&lt;translated name&gt;
&lt;fully qualified class name&gt;.action.&lt;action name&gt;.parameter&lt;index&gt;.description=&lt;translated description&gt;
&lt;fully qualified class name&gt;.action.&lt;action name&gt;.parameter&lt;index&gt;.help=&lt;translated help&gt;</programlisting>

        <para>The class name must be fully qualified and the action name is
        the short name provided by the reflector. All the values are case
        sensitive. The following shows the translated action name and
        description, and two parameter names being specified for the action
        <emphasis>createPhone</emphasis>, which is defined by the
        <literal>createPhone</literal> method.</para>

        <programlisting format="linespecific">example.dom.Contact.action.createPhone.name=Nouveau téléphone
example.dom.Contact.action.createPhone.parameter1.name=Indicatif de zone
example.dom.Contact.action.createPhone.parameter2.name=Nombre
example.dom.Contact.action.createPhone.description=Créez un nouveau téléphone et ajoutez-le au contact actuel</programlisting>
      </section>
    </section>

    <section>
      <title>Viewer Properties</title>

      <para>The look of the viewing mechanism can be changed by using
      different fonts, colours and icons. All viewer properties for the drag
      and drop user interface have a common root of
      <literal>isis.viewer.dnd</literal> and for the HTML interface
      <literal>isis.viewer.html</literal>.</para>

      <section>
        <title>Initial size and location</title>

        <para>The size and location of the application within the windowing
        systems can be specified using the <literal>initial.size</literal> and
        <literal>initial.location</literal> properties. If not specified the
        size defaults to nearly full screen and location to near the top-left
        corner.</para>

        <programlisting format="linespecific">isis.viewer.dnd.initial.size = 800 x 600
isis.viewer.dnd.initial.location = 100, 200</programlisting>
      </section>

      <section>
        <title>Large icon size</title>

        <para>The width of the resource icons (as shown on the desktop) can be
        specified using the <literal>large-icon-size</literal> property. If
        not specified it will default to 34 pixels.</para>

        <programlisting format="linespecific">isis.viewer.dnd.large-icon-size = 48</programlisting>
      </section>

      <section>
        <title>Resize border</title>

        <para>The amount of space allocated for the resize border on a text
        field can be set using the <literal>field-resize-border</literal>
        property. Unless set this will default to 5 pixels.</para>

        <programlisting format="linespecific">isis.viewer.dnd.field-resize-border = 3</programlisting>

        <para>The amount of space allocated for the resize border on a tree
        view can be set using the <literal>tree-resize-border</literal>
        property. Unless set this will default to 5 pixels.</para>

        <programlisting format="linespecific">isis.viewer.dnd.tree-resize-border = 3</programlisting>
      </section>

      <section>
        <title>Exploration menu options</title>

        <para>To turn off exploration menus set the
        <literal>show-exploration</literal> property to
        <emphasis>off</emphasis>.</para>

        <programlisting format="linespecific">isis.viewer.dnd.show-exploration = off</programlisting>
      </section>

      <section>
        <title>Double buffering</title>

        <para>The whole viewer is double buffered by default, but this can be
        turned off by setting the <literal>double-buffer</literal> property to
        <emphasis>off</emphasis>.</para>

        <programlisting format="linespecific">isis.viewer.dnd.double-buffer = off</programlisting>
      </section>

      <section>
        <title>Background logo</title>

        <para>A background logo can be added to the workspace using the
        <literal>logo-background</literal> properties. The image sub-property
        indicates that a logo should be displayed and what image to use. The
        size and location are then optional.</para>

        <programlisting format="linespecific">isis.viewer.dnd.logo-background.image = background.jpg
isis.viewer.dnd.logo-background.size = 400 x 300
isis.viewer.dnd.logo-background.location = 100, 200</programlisting>
      </section>

      <section>
        <title>Loading images from files</title>

        <para>Images are normally loaded directly from the Java resources
        (accessed via the class path) or as files within the <filename
        class="directory" moreinfo="none">images</filename> directory. If
        necessary the loading of files can be suspended so they are only
        loaded from resources. This is done via the
        <literal>load-images-from-files</literal> property.</para>

        <programlisting format="linespecific">isis.viewer.dnd.load-images-from-files = false</programlisting>
      </section>

      <section>
        <title>Padding</title>

        <programlisting format="linespecific">isis.viewer.dnd.hpadding = 5
isis.viewer.dnd.vpadding = 5</programlisting>
      </section>

      <section>
        <title>Default image</title>

        <para>The default image is the one used if the image file/resource
        that should be used cannot be found. This simply ensures that an image
        is available for all icons. The default image can be changed using the
        <literal>default-image</literal> property.</para>

        <programlisting format="linespecific">isis.viewer.dnd.default-image = square.png</programlisting>
      </section>

      <section>
        <title>Image directory</title>

        <para>The directory in which the images are to be found can be
        specified via the <literal>image-directory</literal> property.</para>

        <programlisting format="linespecific">isis.viewer.dnd.image-directory = graphics</programlisting>
      </section>

      <section>
        <title>View specifications</title>

        <para>The views used to render objects and collection are created by
        view specifications. These specifications are installed at start up
        and can be added to. The <literal>specification.view</literal>
        property lists the specifications to load.</para>

        <programlisting format="linespecific">isis.viewer.dnd.specification.view = org.apache.isis.viewer.skylark.basic.FormSpecification, \
        org.apache.isis.viewer.skylark.basic.ListSpecification</programlisting>

        <para>The viewer has a set of default set of views that it loads up,
        these include specifications for forms, lists, tree browsers and
        tables. To disable this default set, normally so you can explicitly
        set up the specification list as above, use the
        <literal>specification.defaults</literal> property with the
        <emphasis>false</emphasis> value.</para>

        <programlisting format="linespecific">isis.viewer.dnd.specification.defaults =off
</programlisting>

        <para>Specific field and subview types can be also specified, so they
        no longer use the built-in default. These all have the same property
        name root of <literal>isis.viewer.dnd.specification</literal>
        and are:-</para>

        <itemizedlist>
          <listitem>
            <para><literal>field.image</literal></para>
          </listitem>

          <listitem>
            <para><literal>field.color</literal></para>
          </listitem>

          <listitem>
            <para><literal>field.password</literal></para>
          </listitem>

          <listitem>
            <para><literal>field.wrappedtext</literal></para>
          </listitem>

          <listitem>
            <para><literal>field.checkbox</literal></para>
          </listitem>

          <listitem>
            <para><literal>field.text</literal></para>
          </listitem>

          <listitem>
            <para><literal>field.empty</literal></para>
          </listitem>

          <listitem>
            <para><literal>icon.subview</literal></para>
          </listitem>

          <listitem>
            <para><literal>icon.object</literal></para>
          </listitem>

          <listitem>
            <para><literal>icon.resource</literal></para>
          </listitem>
        </itemizedlist>

        <para>The following example causes all logical properties to be shown
        using the standard text field rather than the default check
        box.</para>

        <programlisting format="linespecific">isis.viewer.dnd.specification.field.checkbox = \
        org.apache.isis.viewer.skylark.basic.TextFieldSpecification</programlisting>
      </section>

      <section>
        <title>Fonts</title>

        <para>Fonts can be specified using the following font property
        names:-</para>

        <itemizedlist>
          <listitem>
            <para><literal>control</literal>, for the text in control widgets
            such as buttons</para>
          </listitem>

          <listitem>
            <para><literal>label</literal>, for the field labels</para>
          </listitem>

          <listitem>
            <para><literal>large-icon</literal>, for the icons on the desktop
            such as resources</para>
          </listitem>

          <listitem>
            <para><literal>menu</literal>, for the menu options</para>
          </listitem>

          <listitem>
            <para><literal>normal</literal>, for edit fields and object labels
            in fields</para>
          </listitem>

          <listitem>
            <para><literal>small-title</literal>, for windows bars</para>
          </listitem>

          <listitem>
            <para><literal>status</literal>, for text on the status bar</para>
          </listitem>

          <listitem>
            <para><literal>title</literal>, for titling objects</para>
          </listitem>
        </itemizedlist>

        <para>A particular font can be specified for an area by
        specifying</para>

        <programlisting format="linespecific">org.apache.isis.viewer.dnd.font.&lt;area&gt;=&lt;font&gt;</programlisting>

        <para>Where the <literal>&lt;area&gt;</literal> is the name and the
        <literal>&lt;font&gt;</literal> is specified according the
        <literal>Font.decode()</literal> specification (<literal>see
        java.awt.Font</literal>). This essentially is a concatenation of the
        name, style and size as below:</para>

        <programlisting format="linespecific">&lt;logical font name&gt;-&lt;style&gt;-&lt;point size&gt;</programlisting>

        <para>In addition to the font the amount of space between lines of
        text can also be specified.</para>

        <programlisting format="linespecific">org.apache.isis.viewer.dnd.spacing.&lt;area&gt;=&lt;pixels&gt;</programlisting>
      </section>

      <section>
        <title>Colours</title>

        <para>The colour properties are prefixed with
        <literal>isis.viewer.dnd.color</literal> and take a numerical
        value. The most useful form is in hexadecimal where the six digits are
        grouped into twos to represent the red, green and blue components. The
        following example specifies the colours green, red and black
        respectively.</para>

        <literallayout>isis.viewer.dnd.color.normal=0x0000FF
isis.viewer.dnd.color.label=0xFF0000
isis.viewer.dnd.color.text.edit=0xFFFFFF</literallayout>

        <para>The basic colour scheme is made up of a palette of eight
        colours: three primary, three secondary and black and white. These
        form the basis for many of the other colour properties. The colour
        property names are:-</para>

        <itemizedlist>
          <listitem>
            <para><literal>black</literal></para>
          </listitem>

          <listitem>
            <para><literal>white</literal></para>
          </listitem>

          <listitem>
            <para><literal>primary1</literal></para>
          </listitem>

          <listitem>
            <para><literal>secondary1</literal></para>
          </listitem>

          <listitem>
            <para><literal>primary2</literal></para>
          </listitem>

          <listitem>
            <para><literal>secondary2</literal></para>
          </listitem>

          <listitem>
            <para><literal>primary3</literal></para>
          </listitem>

          <listitem>
            <para><literal>secondary3</literal></para>
          </listitem>
        </itemizedlist>

        <para>The backgrounds of the application, the windows and the menus
        can be set with the following properties:-</para>

        <itemizedlist>
          <listitem>
            <para><literal>background.application</literal></para>
          </listitem>

          <listitem>
            <para><literal>background.window</literal></para>
          </listitem>

          <listitem>
            <para><literal>background.content-menu</literal></para>
          </listitem>

          <listitem>
            <para><literal>background.value-menu</literal></para>
          </listitem>

          <listitem>
            <para><literal>background.view-menu</literal></para>
          </listitem>

          <listitem>
            <para><literal>background.workspace-menu</literal></para>
          </listitem>
        </itemizedlist>

        <para>In addition to the setting of a colour for all window
        backgrounds you can also set the colour for a specific view
        type.</para>

        <programlisting format="linespecific">isis.viewer.dnd.color.background.window.List=0x00ffff</programlisting>

        <para>The colours of the text on the menus can be specified with the
        following properties:-</para>

        <itemizedlist>
          <listitem>
            <para><literal>menu.normal</literal></para>
          </listitem>

          <listitem>
            <para><literal>menu.disabled</literal></para>
          </listitem>

          <listitem>
            <para><literal>menu.reversed</literal></para>
          </listitem>
        </itemizedlist>

        <para>The state of the views can be reflected by its colour. The
        following colour properties can be specified (only the identified
        property is based on the core colour scheme, all others are unique by
        default):-</para>

        <itemizedlist>
          <listitem>
            <para><literal>identified</literal></para>
          </listitem>

          <listitem>
            <para><literal>valid</literal></para>
          </listitem>

          <listitem>
            <para><literal>invalid</literal></para>
          </listitem>

          <listitem>
            <para><literal>active</literal></para>
          </listitem>

          <listitem>
            <para><literal>error</literal></para>
          </listitem>

          <listitem>
            <para><literal>out-of-synch</literal></para>
          </listitem>
        </itemizedlist>

        <para>There are a number of properties relating to text for text
        fields. These are the colour of text when editing it, the cursor's
        colour, the colour of the highlight when selecting text, and the
        colour of text when it has focus for editing but has not been changed
        yet.</para>

        <itemizedlist>
          <listitem>
            <para><literal>text.edit</literal></para>
          </listitem>

          <listitem>
            <para><literal>text.cursor</literal></para>
          </listitem>

          <listitem>
            <para><literal>text.highlight</literal></para>
          </listitem>

          <listitem>
            <para><literal>text.saved</literal></para>
          </listitem>
        </itemizedlist>
      </section>

      <section>
        <title>Echo character</title>

        <para>The single character used to represent an entered character in a
        password field can be specified with the <literal>echo</literal>
        property, for example to change it from the default * to a hash
        specify the following:</para>

        <programlisting format="linespecific">isis.viewer.dnd.echo=#</programlisting>
      </section>

      <section>
        <title>HTML viewer</title>

        <para>The html viewer has only two properties</para>

        <programlisting format="linespecific">isis.viewer.html.debug=true</programlisting>

        <para>Which enables debugging on the web controller.</para>

        <programlisting format="linespecific">isis.viewer.html.encoding=UTF-8</programlisting>

        <para>Which enables the character set encoding used by the
        HTTPServletRequest. This is ISO-8859-1 by default.</para>
      </section>

      <section>
        <title>Embedded Web Server</title>

        <para>The embedded web server (implemented by Jetty) has only one
        properties</para>

        <programlisting format="linespecific">isis.embedded-web-server.port=8080</programlisting>

        <para>Which specifies the port to use when prototyping.</para>
      </section>
    </section>

    <section>
      <title>Other Properties</title>

      <section>
        <title id="locale">Locale</title>

        <para>The locale that the system works in can be set by the following
        property:</para>

        <programlisting format="linespecific">isis.locale = en_GB</programlisting>

        <para>The locale is specified in two parts separated by an
        underscore.</para>

        <para>The first part is the language and is a valid ISO Language Code.
        These codes are the lower-case, two-letter codes as defined by
        ISO-639. You can find a full list of these codes at a number of sites,
        such as: <ulink
        url="???">http://www.loc.gov/standards/iso639-2/php/English_list.php</ulink>
        The second part is the country and is a valid ISO Country Code. These
        codes are the upper-case, two-letter codes as defined by ISO-3166. You
        can find a full list of these codes at a number of sites, such as:
        <ulink
        url="???">http://www.iso.ch/iso/en/prods-services/iso3166ma/02iso-3166-code-lists/list-en1.html</ulink></para>

        <para>If no underscore is used then the setting is deemed to be the
        language.</para>

        <para>If no locale is specified the system uses the locale specified
        by the operating system.</para>
      </section>

      <section>
        <title>Time zone</title>

        <para>The time zone that the system works in can be set by the
        following property:</para>

        <programlisting format="linespecific">isis.timezone = Europe/London</programlisting>

        <para>The time zone is normally a geographic area and a location
        separated by a slash (/), such as
        <emphasis>America/New_York</emphasis>,
        <emphasis>Europe/Paris</emphasis> or
        <emphasis>Australia/Perth</emphasis>. These codes can be looked up
        online on sites such as <ulink
        url="http://www.convertit.com/Go/ConvertIt/World_Time/Current_Time.ASP">ConvertIt.com</ulink>
        and <ulink url="http://twiki.org/cgi-bin/xtra/tzdatepick.html">Date
        and Time Gateway</ulink>.</para>

        <para>Alternatively the time zone can be specified in relative format
        of the form <emphasis>GMT-8:00</emphasis> where the relative number of
        hours and minutes can be specified.</para>
      </section>
    </section>
  </section>
</article>
