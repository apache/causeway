!include CommonCreateClaimForTomAndSam

!|using naked objects viewer|
|on object|alias as|perform|using member|
|Employee:Tom Brown|tomsClaims|get collection|Most Recent Claims|

!2 Remove

remove Tom's claim to Tom's collection

!|using naked objects viewer|
|on object|perform|using member|with arguments|
|Employee:Tom Brown|remove from collection|Most Recent Claims|tomsClaim1|


check removed

!|using naked objects viewer|
|on object|perform|using member|that it|with arguments|
|Employee:Tom Brown|check collection|Most Recent Claims|does not contain|tomsClaim1|


!2 Add

add Tom's claim back into Tom's collection

!|using naked objects viewer|
|on object|perform|using member|with arguments|
|Employee:Tom Brown|add to collection|Most Recent Claims|tomsClaim1|



check added

!|using naked objects viewer|
|on object|perform|using member|that it|with arguments|
|Employee:Tom Brown|check collection|Most Recent Claims|contains|tomsClaim1|


